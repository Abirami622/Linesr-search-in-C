
int main()
{
    int i, n, a[100], ele, found=1;
    scanf("%d", &n);
    for(i=0;i<=n;i++)
    {
        scanf("%d", &a[i]);
        
    }
    scanf("%d", &ele);
    for(i=0;i<=n;i++)
    {
        if(a[i]==ele)
        {
            printf("Element found");
            found = 1;
            break;
        }
    }
    if(found!=1)
    {
        printf("Not Found");
    }
}
